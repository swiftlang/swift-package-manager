import Foundation
import BasicMixedTarget

public struct JunkYard {
    // The below types come from the `BasicMixedTarget` module.
    var newCar: NewCar?
    var engine: Engine?
    var oldCar: OldCar?
    var driver: Driver?
}
